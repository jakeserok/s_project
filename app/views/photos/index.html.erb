<h1>Photos</h1>

<% @photos.each do |p| %>
  <div class="center">
    <strong><%= p.title %></strong>
  </div>
  <br/>
  <div class="center-img">
    <%= link_to photo_path(p) do %>
      <%= image_tag p.image, class: "center_img index" %>  
    <% end %>
  </div>
  <br/>
  <hr/>
<% end %>

<%= link_to "Add Photos", new_photo_path %>